// A StaticLot is a div that will hold other components
// Content will be aligned vertically
import styled from 'styled-components'

const StaticLot = styled.div`
  display: flex;
  flex-direction: column;
`
export default StaticLot
